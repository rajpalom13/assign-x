"use client";

import { CreatePostForm } from "@/components/campus-connect";

/**
 * Create Post Page
 * Form for creating new campus connect posts
 * Only accessible to verified college students
 */
export default function CreatePostPage() {
  return <CreatePostForm />;
}
