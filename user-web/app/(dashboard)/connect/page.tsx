"use client";

import { ConnectPro } from "./connect-pro";

/**
 * Campus Connect / Marketplace page
 * Premium SAAS-style design with glassmorphism
 * Header is now rendered by the dashboard layout
 */
export default function ConnectPage() {
  return <ConnectPro />;
}
